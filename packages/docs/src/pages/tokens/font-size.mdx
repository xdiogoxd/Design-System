import { Meta } from '@storybook/addon-docs';
import { TokensGrid } from '../../components/TokensGrid';
import { fontSizes } from '@ignite-ui/tokens';

<Meta title='Tokens/FontSize' description='FontSize tokens' />

# FontSize

Esses são os tamanhos das fontes utilizados no Design System do Ignite UI.

<TokensGrid tokens={fontSizes} />
