import { Meta } from '@storybook/addon-docs';
import { TokensGrid } from '../../components/TokensGrid';
import { space } from '@xdiogoxd-ui/tokens';

<Meta title='Tokens/Space' description='Space tokens' />

# Space

Esses são os espaçamentos utilizados no Design System do Ignite UI.

<TokensGrid tokens={space} />
